export const environment = {
  production: false,
  apiUrl: 'http://localhost:5262/api'
  // 👆 Change 5000 to whatever port your .NET runs on locally
  // Check your .NET launchSettings.json to find your port
};